rules:
  - id: insecure-sql-injection
    patterns:
      - pattern: $SQL = "SELECT * FROM $TABLE WHERE $FIELD = '$USER_INPUT'";
    message: "Potential SQL Injection: User input is directly used in SQL queries."
    languages: [php]
    severity: ERROR
